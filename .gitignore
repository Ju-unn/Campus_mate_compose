# graphify 지식 그래프 — 생성물이라 커밋하지 않는다. 각 PC 에서 /graphify . 로 빌드 (docs/GRAPHIFY.md)
graphify-out/

# OS/에디터
.DS_Store
Thumbs.db
.idea/
.vscode/
*.iml

# Python (backend)
backend/.venv/
backend/**/__pycache__/
backend/**/*.pyc
backend/.pytest_cache/
backend/*.egg-info/
backend/.env

# Firebase 앱 설정 파일 — 프로젝트 식별자가 들어 있어 저장소에 올리지 않는다(조각 4).
**/google-services.json
**/GoogleService-Info.plist
